[package]
name = "braine_viz"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
slint = "1.14"
braine = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
slint-build = "1.14"
